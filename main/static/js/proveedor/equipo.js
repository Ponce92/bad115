function agregar(codigo,nombre){
    $('#nombreE').val(nombre);
    $('#equipo').val(codigo);
}

function eliminar(codigo,nombre) {
    $('#codigo_el').val(codigo);
    $('#sp_nombre').html(nombre);

    
}